# 灵界（LingJie）设计文档

> 目标：为已完成接入的"灵界"维度（`lingjie` 包 + `/frxxlingjie` 传送 + 高空伤害）设计核心玩法规则。
> 本文档只做设计，不写代码。实现依据全部来自对 `frxx-decompiled` 反编译源码的**只读调研**。
> 版本基线：Forge 1.12.2-14.23.5.2860。

---

## 0. 现状（已完成）

- 维度：`LingJieDimension` + `WorldProviderLingJie`，存档目录 `LingJie`，`canRespawnHere=false`，地形复用主世界生成器。
- 准入：`/frxxlingjie`（`/flj`）跨维度传送（`changeDimension` + `connection.setPlayerLocation`）；**主世界高度 > 50000 格自动破界进入灵界**（`LingJieHeightDamageHandler` 内 `teleportToLingJie`）。
- 高空伤害：主世界 y∈[20000, 50000]，面板伤害 =（`攻击`+`法攻`）×3.0，隔 20 tick 一次，无视护甲、绝对伤害，入区仅黄字警告一次。
- 灵界矿脉：`LingJieOreGenerator`（`GameRegistry.registerWorldGenerator`）—— 低阶频率×5、矿脉×5；高阶频率×3、矿脉×3；每区块约1%概率巨型矿脉（人界×20=120格，夹带高阶）。
- 本设计围绕该维度补充：**矿物分布、灵裕值、刷怪、寿元倍率**。

---

## 1. 矿物生成

### 1.1 调研结论（灵石系统全部为"本体"自带，无需自建方块）

经全库检索，**"灵石矿石"在本体的注册名为 `yvanchuxiuzhen:fangkuailingshiXX`**，且生成方式为 `WorldGenMinable` 矿脉：

| 名称 | 注册名 | 来源码 | 原生成矿脉大小 | 定位 |
|---|---|---|---|---|
| 灵石矿石·低阶 | `yvanchuxiuzhen:fangkuailingshi03` | `BlockFangkuailingshi03` | 6（较密） | **灵界主量矿脉** |
| 灵石矿石·高阶 | `yvanchuxiuzhen:fangkuailingshi05` | `BlockFangkuailingshi05` | 4（稀有） | **灵界稀有矿脉** |
| 方块灵石08 / 09 | `fangkuailingshi08/09` | `…08/09` | 无矿脉生成（装饰） | — |

> 伸手可得的其他"修仙自带"料（可选联动）：灵石块 `ling_shi_kuai_1/2`、灵石砖 `ling_shi_zhuan_1/2`、功法·元磁矿 `yvanchujiguan:yuancikuang`（矿脉3）、符阵·朱砂矿 `zhushakuang`（矿脉4）、炼器·闪月银矿等。

### 1.2 灵界矿物分布设计方案

在一层约 256 格高度的标准层级内，按垂直高度分层，**灵石矿极密集**：

| 层 | 高度区间 | 目标矿物 | 分布策略 | 相对密度 |
|---|---|---|---|---|
| 天层（浮空/高海拔） | y 128–192 | 高阶灵石矿 05 | 稀疏矿簇，点缀 | 稀有 |
| 中层 | y 48–96 | 灵界主矿脉：低阶灵石矿 03 | **主量**，极大密度 | 极多 |
| 深层 | y 8–48 | 低阶灵石矿 03 + 装饰灵石块 | 密集层 + 灵石块/砖缀饰 | 多 |
| 熔岩层 | y 0–8 | 中阶资源（可选 08） | 少 | 少量 |

**核心原则**：
1. **低阶灵石矿极密集：频率 = 人界 ×5（每区块 100 次），矿脉 = 人界 ×5（30 格）** —— 灵石矿密度远高于人界。
2. **高阶灵石矿 = 人界 ×3（频率每区块 3 次，矿脉 12 格）** —— 稀有度相对较高但常见率与矿脉都是人界三倍。
3. **巨型矿脉：极小概率（每区块约 1%）触发，矿脉 = 人界 ×20（120 格）**，低阶为主并夹带高阶。
4. 全部复用 `fangkuailingshi03/05` 方块，**不新增方块注册**。
5. 生成时机：仅在 `LingJieDimension.DIMENSION_ID` 维度生效（`LingJieOreGenerator`），完全与主世界分离，不污染主世界矿藏。

---

## 2. 灵裕值机制

### 2.1 调研结论（灵裕系统已有，直接复用键名）

| NBT 键 | 类型 | 含义 | 关键读写点 |
|---|---|---|---|
| `LingYuZhi` | double | 灵裕当前值 | 功法 `ProcedureLianYuZhiAdd`（回复）、本体 `ProcedureAttributeBack`/`ProcedureJingJieReset`（初始化 1000） |
| `LingYuZhiMax` | double | 灵裕上限 | `ProcedureJingLiZhi`（结丹相关写 2000）等 |
| `灵裕值增加计时` | double | 回复计数器 | `ProcedureLianYuZhiAdd` |

现有回复逻辑（`ProcedureLianYuZhiAdd`，功法模组）：
- 每 tick：若 `LingYuZhi < LingYuZhiMax`，则计数器 +1；
- 计数 ≥ 24000（=1 游戏日）→ `LingYuZhi +50`，计数器清零；若超上限则钳制到上限。
- 即主世界事实规则：**上限 1000，每日回复 50**。

### 2.2 灵界设计目标（用户需求）

> 上限 **100000**，每日回复 **10000**。

设计要点：
1. **上限 100000**：进入灵界时（`/flj` 传送落地后 / `PlayerTickEvent` 维度判定）将 `LingYuZhiMax` 提升到 100000（建议写独立标志位区分，避免污染主世界上限）。
2. **每日回复 10000**：灵界内不直接改 `ProcedureLianYuZhiAdd`（跨模组改动风险），而由我们的**独立 Procedure** 承担：在灵界维度每 tick 若 `LingYuZhi < LingYuZhiMax` 则 `灵裕值增加计时` +1，计满 24000 → `+10000` 并清零。
   - 为避免与主世界 +50 逻辑同时触发，灵界内由本方逻辑覆盖；若需干净隔离，可让本方在灵界维度内接管该键的写权。
3. **回主世界还原**：离开灵界回主世界时，把 100000 上限还原到原值（或按原境界规则），并在临界处钳制当前值不越新上限。
4. 保持钳制语义与本体一致（当下值 < 上限才累加；加完过限则置为上限制）。

---

## 3. 刷怪方案（妖兽/修士全部结丹级以上）

### 3.1 调研结论（实体全部来自"妖兽"模组 yvanchuyaoshou）

妖兽模组共 75 个 Entity 类，**全模组复用统一 NBT 属性体系**：实体自定义名含 `炼气/筑基/结丹/元婴…` + `初期/中期/后期` 驱动境界属性（`攻击`/`defense`/`法攻`/`法防`/`生命`/`战斗恢复`/`掉落真元` 等，见《修士道友道侣生物调研报告》§3）。

- **修士类（可作"灵界修士"）**：`EntitySanXiu01~05`(+`02` 变体) 散修、`EntityMoXiu01~05`(+`02`) 魔修、`EntityFangShiGuard01/02` 护道者、`EntityHeHuanZong1/2/3` 合欢宗、`EntityTianGong1/2/3`。
- **妖兽类**：`EntityGuLang*`(妖狼)、`EntityBaiYuZhiZhu`/`EntityXueYuZhiZhu`(蜘蛛)、`EntityLeiBanLu*`/`EntityLinHaiShouHuang*`(猎豹/临海兽皇)、`EntityLeiYuanJuE*`(雷元巨鳄)、`EntityYingXiao*`(鹰)、`EntityHunTieShou*`、`EntityShiFengTuo*`、`EntityYuWa*` 等。

### 3.2 灵界刷怪设计

**总规则：灵界刷出的实体——无论是修士还是妖兽——全体为"结丹级以上"，且没有低阶杂怪。**

| 档位 | 实体来源 | 落点境界名 | 刷新权重 |
|---|---|---|---|
| 结丹（主力） | 妖兽模组各位 `EntitySanXiu*` ~`MoXiu*`、各野兽 `EntityGuLang*`/`EntityBaiYuZhiZhu*` 等做宿主 | 自定义名写 `结丹初/中/后期` | **很高** |
| 元婴 | `EntityFangShiGuard01/02`、`EntityLvNi`、高强度野兽 | `元婴初/中/后期` | 中 |
| 化神 | `EntityHeHuanZong2/3`、`EntityTianGong*`、`EntityLeiBanLu05`/`EntityLeiYuanJuE05` 等 05 后缀终极变体 | `化神初/中/后期` | 低（首领向） |

实现要点：
1. **复用现有实体与刷怪注册**，不再新建实体类；通过 `EntityRegistry.addSpawn`（或维度自建刷怪列表）在 `LingJieDimension` 维度挂接。
2. **门槛粒度用"名称关键字"驱动**：生成实体后改写其自定义名（`Entity` 的 `func_200200_i`/`func_174817_o` 对应设置夜样式、含境界段），使其进入 `结丹/元婴/化神` 档，从而让既有 `Procedure*XinXi` 自动套用对应强度（不再写新属性公式）。
3. **高频刷新**：结丹档刷新权重高 → 野外"练手/刷材料"频繁；元婴/化神逐渐稀少；完全**不刷** `炼气/筑基` 名字的怪物（避免与"全部结丹+"目标冲突）。
4. 掉落沿用妖兽模组既有 `Procedure*Die`（真元、储物袋、材料），仅通过境界名回调强度，无需重写掉落。

---

## 4. 寿元倍率（关卡加成）

### 4.1 调研结论（寿元是实体 NBT `ShouYuan`，double）

- 本体进阶时直接给寿元（绝对值）：`ProcedureXiaoJingJieNameSet1/2/3`（+50/100/300/500、+1000/3000/5000/10000、+20000/50000/100000）。
- 自然消耗：`ProcedurePlayerUpdate2` 按 `ConfigShouYuanTime` 每 tick 逐点扣减，归零触发生死倒计时（"汝之寿元所剩为零，留你五分钟交代后事"）。
- 突破失败折损：`ProcedureTuPoShiBaiGaiLvAdd`；丹药副作用：`ProcedureShouYuanCut/Cut2`；助手可读/加寿元 `ProcedureShouYuan/2`。

> 即本游戏寿元机制是"**加法绝对值**"，遗愿为**乘法倍率** —— 需要在我们层级做一次转换。

### 4.2 灵界设计目标（用户需求倍率）

| 现实境界 | 灵界寿元倍率 |
|---|---|
| 凡人 | 1.5× |
| 练气 | 2.5× |
| 筑基 | 2.5× |
| 结丹 | 3.5× |
| 元婴 | 4.5× |
| 化神 | 5.5× |

设计要点：
1. 以玩家当前境界（读本体境界 NBT，如 `修士境界`/`修炼境界`）查上表倍率。
2. 采用"**基准寿元 × 倍率 - 基准寿元 = 灵界加成**"的差量法：本体进阶已给绝对寿元，灵界代表"该境界在灵界额外多活 × 倍"，故给 `ShouYuan += base × (multiplier − 1)`。
   - 例：练气 base=100 ÷ 本体，灵界再补 `100×(2−1)=100` → 总 200（即 2×）。
3. 基准 base 建议取本体该境界的默认寿元（数值来自 `ProcedureXiaoJingJieNameSet*` 对应档），确保"翻倍"可解释、可控。
4. 灵界加成在进入灵界时结算一次（写标志防重复），离开时保持（属于永久增益记忆），体现"在灵界历练延寿"。

---

## 5. 风险与后续注意

- **维度生成分离**：矿物与刷怪挂钩必须限定 `Dimension id = lingjie`，严禁回灌主世界。
- **灵裕双写冲突**：主世界 `ProcedureLianYuZhiAdd`（+50）与灵界独立 Procedure（+10000）需用维度/标志位隔离，避免同 tick 双触发。
- **寿元是实体数据而非境界表**：倍率需在"进入灵界"这个确定节点追加，先清点玩家旧寿元与记忆标志，避免重复加。
- **刷怪境界靠名称驱动**：改实体自定义名前先确认对应 `Procedure*XinXi` 的关键字分支已覆盖 `结丹/元婴/化神`，否则生成的实体不进入目标强度档。

---

## 6. 待确认清单（实现前需答复）

1. 矿物：是否仅用低阶 03 + 高阶 05 两种？是否联动 08/灵石块做装饰？矿脉密度/大小给定一个默认值即可进入实现。
2. 灵裕：离开灵界回主世界时，上限 100000 是否要还原（还原到原境界规则值）？还是永久保留在该档？
3. 刷怪：以妖兽模组的 75 实体中"修士系 + 野兽系"为宿主、靠改自定义名抬结丹+，此方案是否接受？是否要排除某些实体（如 HeHuanZong/TianGong 只做化神首领）？
4. 寿元：差量 `base×(multiplier−1)` 方式是否接受？基准 base 以本体默认档为准可以吗？