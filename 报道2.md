# 《凡人修仙·属性丹药》材质注册专项报道

## 一、单数 `item` 与复数 `items` 有何不同

这是 Minecraft 资源包命名空间的**目录约定**，不是代码机制差异：

| 写法 | 实际资源路径 | 状态 |
|---|---|---|
| `"layer0": "frxxmengzhi:item/xxx"` | `assets/frxxmengzhi/textures/item/xxx.png` | ❌ 非标准，纹理加载异常 |
| `"layer0": "frxxmengzhi:items/xxx"` | `assets/frxxmengzhi/textures/items/xxx.png` | ✅ 1.12.2 标准约定 |

- 模型 JSON 里 `layer0` 的值会**原样**拼接成纹理路径：`<域>:textures/<layer0值>.png`，不自动加复数
- 原版 Minecraft 的物品纹理一律在**复数** `textures/items/` 目录（如苹果 `items/apple.png`），模型 `models/item/*.json` 用的是**单数** `item/` 目录
- 我们之前建成了 `textures/item/`（单数）→ 模型引用 `frxxmengzhi:item/xxx` 时找到的文件与 atlas 加载规则冲突，渲染紫黑
- 实验验证：引用 `minecraft:item/apple`（单数）→ 紫黑；`minecraft:items/apple`（复数）→ 正常显示苹果
- **结论**：模型 JSON 目录是单数 `models/item/`，纹理目录必须是复数 `textures/items/`

## 二、Java 注册材质相关代码总结

### 1. 物品注册 — `elixir/ElixirRegistry.java`
- `@Mod.EventBusSubscriber` + `@SubscribeEvent` 监听 `RegistryEvent.Register<Item>`（Forge 现代注册方式，非废弃的 GameRegistry）
- 注册流程：构造物品 → 自动 `setRegistryName("frxxmengzhi", 名称)` → `registry.register(物品)`
- 注册名即模型文件名（一一对应），Forge 自动绑定 `ModelResourceLocation(注册名, "inventory")` 并加载 `models/item/<注册名>.json`

### 2. 注册名生成 — `elixir/ItemElixirBase.java`
```java
setRegistryName("frxxmengzhi", getElixirRegistryName());
// 名称 = (perm_ | temp_) + 属性id + "_" + 境界短名
// 例：perm_shield_max_lianqi（永久·护盾上限·练气）
```
- 本地化键：`item.frxxmengzhi.<注册名>.name`（已修复 .name 后缀问题）

### 3. 物品类分工
- `ItemElixirPermanent`（永久丹）、`ItemElixirTemporary`（临时灵丹）、`ItemAttributeAssistant`（属性助手）→ 均继承 `ItemElixirBase`，**无任何渲染/模型覆写**
- 材质关联完全依赖资源文件名 = 注册名（零代码配置）

### 4. 本次修复内容
- `textures/item/` → `textures/items/`（31 个 PNG 全部迁移）
- 32 个模型 JSON：`"parent": "item/generated"` → `"minecraft:item/generated"`（等价写法，规范化）
- 所有 `layer0`：`frxxmengzhi:item/xxx` → `frxxmengzhi:items/xxx`
- 还原诊断残留（perm/temp_shield_max_lianqi.json）

## 三、31 个物品注册清单（注册名 = 模型文件名 = 纹理文件名）

### 永久丹药（15 个，类 ItemElixirPermanent，ID 4096-4110）
| 注册名 | 中文名 |
|---|---|
| perm_shield_max_lianqi | 练气·护盾上限丹 |
| perm_shield_regen_lianqi | 练气·护盾回复丹 |
| perm_shield_absorption_lianqi | 练气·伤害吸收丹 |
| perm_shield_max_zhiji | 筑基·护盾上限丹 |
| perm_shield_regen_zhiji | 筑基·护盾回复丹 |
| perm_shield_absorption_zhiji | 筑基·伤害吸收丹 |
| perm_shield_max_jiedan | 结丹·护盾上限丹 |
| perm_shield_regen_jiedan | 结丹·护盾回复丹 |
| perm_shield_absorption_jiedan | 结丹·伤害吸收丹 |
| perm_shield_max_yuanying | 元婴·护盾上限丹 |
| perm_shield_regen_yuanying | 元婴·护盾回复丹 |
| perm_shield_absorption_yuanying | 元婴·伤害吸收丹 |
| perm_shield_max_huashen | 化神·护盾上限丹 |
| perm_shield_regen_huashen | 化神·护盾回复丹 |
| perm_shield_absorption_huashen | 化神·伤害吸收丹 |

### 临时灵丹（15 个，类 ItemElixirTemporary，ID 4111-4125）
| 注册名 | 中文名 |
|---|---|
| temp_shield_max_lianqi | 练气·护盾上限灵丹 |
| temp_shield_regen_lianqi | 练气·护盾回复灵丹 |
| temp_shield_absorption_lianqi | 练气·伤害吸收灵丹 |
| temp_shield_max_zhiji | 筑基·护盾上限灵丹 |
| temp_shield_regen_zhiji | 筑基·护盾回复灵丹 |
| temp_shield_absorption_zhiji | 筑基·伤害吸收灵丹 |
| temp_shield_max_jiedan | 结丹·护盾上限灵丹 |
| temp_shield_regen_jiedan | 结丹·护盾回复灵丹 |
| temp_shield_absorption_jiedan | 结丹·伤害吸收灵丹 |
| temp_shield_max_yuanying | 元婴·护盾上限灵丹 |
| temp_shield_regen_yuanying | 元婴·护盾回复灵丹 |
| temp_shield_absorption_yuanying | 元婴·伤害吸收灵丹 |
| temp_shield_max_huashen | 化神·护盾上限灵丹 |
| temp_shield_regen_huashen | 化神·护盾回复灵丹 |
| temp_shield_absorption_huashen | 化神·伤害吸收灵丹 |

### 工具（1 个，ID 4126）
| 注册名 | 中文名 | 类 |
|---|---|---|
| attribute_assistant | 属性小助手 | ItemAttributeAssistant |

### 每件物品注册内容
- **注册**：`RegistryEvent.Register<Item>` → `registry.register(item)`
- **模型**：`assets/frxxmengzhi/models/item/<注册名>.json`（parent: minecraft:item/generated）
- **纹理**：`assets/frxxmengzhi/textures/items/<注册名>.png`（16×16）
- **本地化**：`item.frxxmengzhi.<注册名>.name=中文名`
- **创建标签页**：`CreativeTabElixir`（"属性丹药"）

（另有诊断物品 test_item，引用 vanilla 苹果纹理，验证通过后可移除）
