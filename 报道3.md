# 《凡人修仙·属性丹药》命名与注册链路全面检查报道

## 一、检查背景与动机

前期排查已证实：模型 JSON 语法合法、PNG 内容含彩色像素、jar 与源码哈希一致、纯 Forge 环境单 mod 仍紫黑。其中 test_item（普通 Item + vanilla 苹果纹理）能正常显示，而 31 个丹药全部紫黑，且 FML 日志不报告 frxxmengzhi 域纹理缺失。用户提出疑问：是否命名或注册环节存在系统性错误。本文对命名与注册链路做逐项全检。

## 二、注册链路全景（代码层）

### 1. 注册入口 — ElixirRegistry.java
- 类上标注 `@Mod.EventBusSubscriber(modid = "frxxmengzhi")`，并由主类 preInit 中 `MinecraftForge.EVENT_BUS.register(ElixirRegistry.class)` 双保险注册
- 监听 `RegistryEvent.Register<Item>`，通过 `event.getRegistry().register(item)` 注册（Forge 现代标准 API，非已废弃的 GameRegistry）
- debug.log 确认 4096~4126 共 31 个物品注册成功，无 ID 冲突

### 2. 注册名生成 — ItemElixirBase.java
```java
setRegistryName("frxxmengzhi", getElixirRegistryName());
// 规则: (perm_|temp_) + 属性id + "_" + 境界短名
```
- ElixirType.getId()：`shield_max` / `shield_regen` / `shield_absorption`（全小写，合法注册名字符）
- ElixirRealm.getShortName()：`lianqi` / `zhiji` / `jiedan` / `yuanying` / `huashen`（全小写）
- 拼接结果如 `perm_shield_max_lianqi`——注册名、模型文件名、纹理文件名、lang 键四者同源生成，无手写错位风险

### 3. 模型绑定 — 零代码
- 未调用 `ModelLoader.setCustomModelResourceLocation`，依赖 Forge 默认绑定 `ModelResourceLocation(注册名, "inventory")`
- test_item 能显示苹果，证明默认绑定链路完全通畅（否则连 test_item 都紫黑）

### 4. 显示名链路
- 覆写 `getItemStackDisplayName` 直接查询 `item.frxxmengzhi.<path>.name`，绕开 Forge 的 unlocalizedName 拼接链（此前 .name 后缀问题的根源），本地化已实测正常

## 三、命名规范逐项检查（全部通过）

| 检查项 | 方法 | 结果 |
|---|---|---|
| 注册名 ↔ 模型文件名 | 枚举生成规则推导 + jar 比对 | 31/31 一致 |
| 模型文件 ↔ 纹理文件 | 目录交叉比对 | 31/31 一一对应 |
| 纹理 ↔ lang 键 | 31 键正则提取比对 | 100% 匹配 |
| 大小写 | PNG/JSON 文件名与引用逐字节比对 | 全小写，零差异 |
| BOM 头 | 读取 32 个 JSON 首 3 字节 | 0 个带 BOM |
| 编码 | lang/JSON UTF-8 无 BOM | 正常 |
| PNG 格式 | 解析 IHDR 头 | 全部 16×16、8bit、RGBA，标准格式 |
| mcmod.info | modid/version/mcversion | 与 @Mod 注解完全一致 |
| 依赖声明 | after:fanrenxiuxian;yvanchuxiuzhen;mixinbooter | 可选依赖，纯 Forge 可运行 |
| 网络通道 | "frxxmz" 6 字符 | 在 20 字符限制内 |
| 注册时机 | RegistryEvent 标准时机 | debug.log 确认注册成功 |

## 四、本次检查中的唯一实际发现

**纹理目录单复数约定错误（已在上一轮修复）**：
- 模型 JSON 目录约定为单数 `models/item/`
- 纹理目录约定为复数 `textures/items/`
- 原项目错误建为 `textures/item/`（单数），与 1.12.2 全游戏约定不符
- 已修复：31 个 PNG 迁移至 `textures/items/`，全部 layer0 引用同步改为 `frxxmengzhi:items/xxx`，parent 统一为 `minecraft:item/generated`
- 修复版已构建（SHA256 `E5F420E5...`）

## 五、命名与注册之外的剩余疑点分析

若修复版实测后丹药仍紫黑，则问题不在资源路径与命名，只剩两个方向：

1. **模型 variant 加载阶段**：目前日志无 "Exception loading model for variant" 错误，但 FML 的 TEXTURE_ERRORS 报告仅来自"模型成功加载后引用的缺失纹理"。丹药若模型加载失败，将静默进入 missing model（完整紫黑方块），与观察现象吻合——需要通过最新一次启动日志复核是否有 frxxmengzhi 相关异常

2. **atlas 纹理装载阶段**：纹理引用正确但未进入 atlas 时，同样渲染为紫黑方块。FML 检查只报告"资源不存在"，不报告"资源存在但装载失败"。若 31 个丹药 PNG 在装载阶段集体失败，日志会给出 `Failed to load texture` 类记录，需查最新日志

## 六、结论与建议

1. **命名、注册、路径、编码、格式六大类检查全部通过**，代码层不存在系统性命名/注册错误
2. 已修复并落地的唯一实质问题：纹理目录复数约定
3. **强烈建议先实测修复版（build9，SHA256 `E5F420E5...`）**：若丹药已显示彩色，问题即全部解决；若仍紫黑，请提供该次启动的 latest.log + debug.log，重点复核第五节的两个疑点方向
4. 待问题解决后，可移除诊断物品 test_item（ElixirRegistry.java 中临时注册段）并清理报道.md 中记录的实验残留

（附：全部检查脚本留存于会话，可随时复跑复核）
