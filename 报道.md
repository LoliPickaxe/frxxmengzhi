# 《凡人修仙·属性丹药》模组资源排查报道

## 一、项目概况
模组ID：`frxxmengzhi`，版本 1.0.0，MC 1.12.2 / Forge 14.23.5.2859。功能：护盾类属性丹药 30 种（永久/临时各 15）+ 创造测试工具"属性小助手"。编译依赖：`libs/前置.jar`、`libs/本体.jar`（仅编译用，代码无运行时引用），构建命令 `gradlew build --offline`。

## 二、Java 文件（src/main/java/com/frxx/mengzhi/，共 18 个）
- `FanRenXiuXianMengZhi.java`：主类 @Mod，注册创造标签页、网络通道、事件订阅
- `elixir/`：`ElixirRegistry`（物品注册）、`ItemElixirBase`（基类，覆写 `getItemStackDisplayName` 修复本地化）、`ItemElixirPermanent`/`ItemElixirTemporary`（永久/临时丹药）、`ItemAttributeAssistant`（属性助手）、`CreativeTabElixir`、`GuiAttributeAssistant`（配置 GUI）、`ElixirTickHandler`
- `handler/`：`ClientHooks`、`GuardClientHandler`、`GuardHandler`、`KnockbackHandler`
- `network/`：`GuardActionPacket`、`GuardStatePacket`
所有物品类均为标准 Item 实现，**无任何模型/渲染覆写**。

## 三、JSON 文件（资源，共 33 个）
- `assets/frxxmengzhi/models/item/*.json`：32 个物品模型（31 丹药 + test_item），统一格式 `parent: item/generated` + `layer0` 引用纹理
- `mixins.frxxmengzhi.json`：**空配置**（client/server/mixins 均为空数组，无实际 mixin）
- `mcmod.info`：mod 元信息

## 四、PNG 文件（共 31 个）
`assets/frxxmengzhi/textures/item/*.png`（**单数 item 目录**），16×16，与模型一一对应；已逐像素验证有彩色内容、jar 与源码哈希一致。

## 五、物品中英文对照（31 种）
命名规则：`perm_`/`temp_` + `shield_max`/`shield_regen`/`shield_absorption` + 境界（lianqi练气/zhiji筑基/jiedan结丹/yuanying元婴/huashen化神）；永久=丹（Elixir），临时=灵丹（Spirit Elixir）。例：
- `perm_shield_max_lianqi` 练气·护盾上限丹 / Qi Refining Shield Max Elixir
- `temp_shield_absorption_huashen` 化神·伤害吸收灵丹 / Spirit Transformation Damage Absorption Spirit Elixir
- `attribute_assistant` 属性小助手 / Attribute Assistant
（15 永久丹 + 15 临时灵丹 + 1 助手，中英文本地化均已在 lang 文件就绪）

## 六、紫黑问题排查结论
1. **已排除**：PNG 内容、模型 JSON 语法、路径存在性、OptiFine、其他 mod 冲突、依赖劫持（纯 Forge 环境单 mod 仍紫黑）
2. **关键突破**：纹理路径格式必须为**复数**——`minecraft:item/apple`（单数）→ 紫黑；`minecraft:items/apple`（复数）→ 正常显示苹果
3. **现状**：test_item 已能显示苹果（证明模型/绑定/渲染链路全通）；丹药引用 `frxxmengzhi:item/xxx`（单数）全部紫黑，且 FML 日志**不报告** frxxmengzhi 域纹理缺失，疑似 mod 自有纹理未进入 atlas 或引用解析异常，**根因尚未最终定位**

## 七、实验残留（构建前需还原）
- `test_item.json`：layer0 指向 `minecraft:items/apple`（诊断用）
- `temp_shield_max_lianqi.json`：layer0 临时改为 `minecraft:items/apple`
- `perm_shield_max_lianqi.json`：layer0 残留 `minecraft:item/apple`（单数，错误路径）
- `FanRenXiuXianMengZhi.java`：依赖声明已由 required-after 改为 after（可独立运行）
- `ElixirRegistry.java`：含临时诊断物品 test_item
